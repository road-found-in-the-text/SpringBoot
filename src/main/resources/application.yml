spring:
  application: name:umc3_teamproject
  datasource:
    # url: jdbc:mysql://localhost:3306/umc3?serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=UTF-8
    url: jdbc:mysql://database-1.c78drytvtz2h.ap-northeast-2.rds.amazonaws.com:3306/umc3?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: text1234!!
  driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB


  #swagger? ?? ???

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show_sql: true
    format_sql: true

  logging:
    level:
      org.hibernate.SQL: debug

  profiles:
    include: oauth

custom:
  config:
    oauth:
      social:
        base_url: http://localhost:8000

        kakao:
          client_id: d5517e911cbbe70ba473c242d40d0407
          callback_path: "/oauth/kakao"
          client-authentication-method: POST
          authorization-grant-type: authorization_code
          scope: profile_nickname, account_email #동의 항목
          client-name: Kakao

          authorization_uri: https://kauth.kakao.com/oauth/authorize
          token_uri: https://kauth.kakao.com/oauth/token
          user_info_uri: https://kapi.kakao.com/v2/user/me
          user-name-attribute: id
#          apple:
#            cient-id: [CLIENT ID (org.example.apple)]
#            team-id: [TEAM ID]
#            #private key는 직접 생성
#            callback_path : /oauth/apple/callback
#
#            authorization_uri: https://appleid.apple.com/auth/authorize
#            token_uri: https://appleid.apple.com/auth/token
#            public_key: https://appleid.apple.com/auth/keys

  mail:
    host: smtp.gmail.com
    port: 587
      # username: road.found.in.the.text@gmail.com
    username: road.found.in.the.text@gmail.com
    password: text1234!!
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
    com:
      amazonaws:
        util:
          EC2MetadataUtils: ERROR

cloud:
  aws:
    credentials:
      accessKey: AKIASMZOOGKFOM3N7Y4X       # AWS IAM AccessKey 적기
      secretKey: RiPqJzkuOqijfDyPckhZzkWdkKPYuD9R+BmtfsEp   # AWS IAM SecretKey 적기
    s3:
      bucket: com.gleonroad.umcproject

    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

