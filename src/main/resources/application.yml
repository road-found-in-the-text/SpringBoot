spring:
  datasource:
    driver-class-name=com:
      mysql:
        cj:
          jdbc:
            Driver:
      url:
        jdbc:mysql://localhost:3306/TEXTROAD
    username: root
    password: root

  #swagger? ?? ???
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      show_sql: true
      format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

